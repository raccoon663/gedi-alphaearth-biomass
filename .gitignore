# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
.venv/
venv/
env/

# Secrets / credentials (never commit)
.env
.env.*
credentials/
secrets/
*.pem
*service*account*.json

# Large or local data products (not distributed through GitHub)
data/raw/
data/processed/
data/interim/
data/manifests/
data/china_field_biomass/
data/boundaries/
outputs/cache/
outputs/logs/
outputs/models/
outputs/predictions/
archive/
work/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Per-sample manifests with GEDI shot IDs / coordinates (excluded from the repo;
# regenerate locally from the frozen Earth Engine exports). The frozen designs,
# folds, seeds, and SHA-256 checksums remain in outputs/manifests/ as proof the
# experiment was not modified after the fact.
outputs/manifests/source_aef_central_input.csv
outputs/manifests/kaihua_fewshot_spatial_folds.csv
outputs/manifests/kaihua_fewshot_label_draws.csv
outputs/manifests/aef_development_subset_75.csv
outputs/manifests/aef_development_common_manifest.csv
# Row-level diagnostic intermediates (repository keeps aggregate tables/figures).
outputs/tables/diagnostics/palsar_source_oof_predictions.parquet
outputs/tables/diagnostics/radar_domain_pca_points.csv
